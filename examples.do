startr

sysuse auto, clear
keep make price mpg rep78

tor
r StataData <- within(StataData, new <- price / 100)
r cat(mean(StataData[["price"]]), "\n")
r cat(mean(StataData\$price), "\n")
fromr, clear
list in 1/5, clean

stopr, clear
